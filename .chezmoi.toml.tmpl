{{- $isWork := or (contains "inatel" .chezmoi.fullHostname) (contains "ICCSO" .chezmoi.fullHostname) (contains "seli" .chezmoi.fullHostname) (contains "vmx" .chezmoi.fullHostname) -}}
{{- $isPersonal := not $isWork -}}
{{- $isWsl := or (env "WSL_DISTRO_NAME") (env "IS_WSL") | not | not -}}
{{- $isRemoteContainers := env "VSCODE_REMOTE_CONTAINERS_SESSION" | not | not -}}
{{- $isGnome := lookPath "gnome-shell" | not | not -}}

sourceDir = "{{ .chezmoi.sourceDir }}"

[data]
  email = "gustavogsb@outlook.com"
  name = "Gustavo Baptista"
  is_personal = {{ $isPersonal }}
  is_work = {{ $isWork }}
  is_wsl = {{ $isWsl }}
  is_remotecontainers = {{ $isRemoteContainers }}
  is_gnome = {{ $isGnome }}
